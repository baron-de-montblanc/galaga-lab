[build-system]
requires = ["setuptools >= 61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "galaga_lab"
version = "0.0.7"
dependencies = [
    "astropy", 
	"numpy", 
	"dash", 
	"dash_bootstrap_components",
    "Flask", 
	"plotly", 
	"PyYAML",
]
